# This .gitignore file ignores the files that don't need to be tracked by git; '*' means don't track those files also beneath the directory the .gitignore file is in

# Windows
*.desktop.ini

# Visual Studio
.vs/

# Visual Studio Code
.vscode/

# DotNet - Just use a trailing slash to specify directories; this recursively ignores all files within these directories 
bin/
obj/

# Mac OS X
*.DS_Store
